let age = 19;
let isLoggedIn = false;

let state = null;

let stringofbool; // undefined

let stateOfNull = null; // object

// symbols are primitive datatype to be used for unique values

const sym1 = Symbol("id");
const sym2 = Symbol("id");

console.log(sym1 === sym2);
const user = {
  name: "DHAIRYA",
  sym1: 1234,
  sym2: 5783,
};

console.log(user);
